const INITIAL_STATE = {
  characters: [],
};

function charactersReducer(state = INITIAL_STATE, action) {
  switch(action.type) {
  default:
    return state;
  }
}

export default charactersReducer;
